#include<stdio.h>
void main()
{
    int a[50],n,i=1,j,temp,rem;
    printf("Enter the no : ");
    scanf("%d",&n);
    temp=n;
    while(temp!=0)
    {
        rem=temp%2;
        a[i]=rem;
        i++;
        temp=temp/2;
    }
    for(j=i-1;j>0;j--)
    {
        printf("%d",a[j]);
    }
}

